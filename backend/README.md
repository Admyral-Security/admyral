# Backend

Important commands:

```bash
# Before starting the server for the first time, run the setup script
./initial_setup.sh

# Start the server
./start_server.sh

# Run tests
poetry run pytest
```
