# Mistral AI

1. Go to [Mistral AI console](https://console.mistral.ai) and log in.

2. Go to **La Plateforme** and create an API key in the settings.

3. Go to the **Settings** page in **Admyral**. Click on **Create New Secret** in the **Secrets** section.

4. Give your credential a name, e.g. `MistralAI`.

5. The following secrets structure is expected:

| Key       | Value                       |
| --------- | --------------------------- |
| `api_key` | Paste your **API key** here |

6. Click on the **Save** button.

Alternatively, you can use the following CLI command structure:

```bash
admyral secret set mistral_secret --value api_key=your_key_value
```
