# AlienVault OTX

1. Go to [AlienVault OTX API Documentation](https://otx.alienvault.com/api). You can find your OTX API key in the top-right.

2. Go to the **Settings** page in **Admyral** and click on **Add New Secret** in the **Secrets** section.

3. Give your secret a name, e.g. `AlienVault`.

4. The following secrets structure is expected:

| Key       | Value                                      |
| --------- | ------------------------------------------ |
| `api_key` | Paste the API key from AlienVault OTX here |

![AlienVault OTX Secrets Setup](/alien_vault_secrets.png)

5. Click on the **Save** button.

Alternatively, you can use the following CLI command structure:

```bash
admyral secret set alien_vault_secret --value api_key=your_key_value
```
